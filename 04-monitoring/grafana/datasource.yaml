apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

## this must be mounted in Grafana using this path...
## /etc/grafana/provisioning/datasources/